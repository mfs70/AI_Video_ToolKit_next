<Window x:Class="AI_Video_ToolKit.UI.Dialogs.FrameBuildOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Build video from frames"
        Width="420"
        Height="230"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#252526">
        <Grid Margin="18">
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Frames folder"
                           Foreground="#9CDCFE"
                           FontWeight="SemiBold"/>
                <TextBlock Grid.Row="1"
                           Text="{Binding FolderPath, RelativeSource={RelativeSource AncestorType=Window}}"
                           Foreground="#D4D4D4"
                           TextTrimming="CharacterEllipsis"
                           Margin="0,4,0,14"/>

                <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                                <TextBlock Text="Quality"
                                           Foreground="#D4D4D4"
                                           Margin="0,0,0,4"/>
                                <ComboBox x:Name="QualityCombo"
                                          SelectedIndex="1">
                                        <ComboBoxItem Content="Low"/>
                                        <ComboBoxItem Content="Medium"/>
                                        <ComboBoxItem Content="High"/>
                                </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                                <TextBlock Text="Format"
                                           Foreground="#D4D4D4"
                                           Margin="0,0,0,4"/>
                                <ComboBox x:Name="FormatCombo"
                                          SelectedIndex="0">
                                        <ComboBoxItem Content=".mp4"/>
                                        <ComboBoxItem Content=".mov"/>
                                        <ComboBoxItem Content=".mkv"/>
                                        <ComboBoxItem Content=".avi"/>
                                </ComboBox>
                        </StackPanel>
                </Grid>

                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,18,0,0">
                        <Button Content="Cancel"
                                Width="90"
                                Margin="0,0,8,0"
                                IsCancel="True"/>
                        <Button Content="Build"
                                Width="90"
                                IsDefault="True"
                                Click="Build_Click"/>
                </StackPanel>
        </Grid>
</Window>
