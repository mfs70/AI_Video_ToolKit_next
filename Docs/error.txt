во время разборки (и во время сборки) видеофайла на кадры должен работать прогрессбар.



стрелка вправо не перемещает курсор на один кадр(стрелка с шифтом перемещает на 10 кадров, правильно)
стрелка влево(и с шифтом) работает. 
прогрессбар некорректно отображает прогресс выполнения экспорта. при обработке большого файла,
который необходимо разделить на множество сегментов, приложение может "зависать",
и недавать ничего делать в UI.
прогрессбар всегда виден в интерфейсе, и когда доходит до 100%, то так и остается в этой позиции,
он должен появляться только после нажатия кнопкок экспортолл или экспортселл,
доходить до конца процесса 100%, далее писать строчку в логе (например все сегменты готовы) и проподать из интерфейса.

сделать кнопки в панели действий, по стилю такими же как в панели управления плеера"круглыми".
добавить в панель действий кнопки собрать из кадров(собирает видеоролик из кадров в папке Frames и помешает результат в папку Output), 
разобрать на кадры(разбирает загруженный видеоролик на кадры и помещает в папку Frames),
склеить (склеивает подряд все ролики из монтажной панели)
склеить выделенные.
при отсутствии "действий" под кнопками писать, писать лог действий(нажата кнопка такая то итд).
помещать разрезаные сегменты на монтажный стол(монтажную панель).


экспорт когда 

экспорт сегментов не работает (и всех сегментоа и выделенного сегмента)




При выборе любого установленного маркера мышкой, он меняет цвет,
и его можно перемещать по таймлайну мышкой и стрелками влево вправо,
но убрать фокус с текущего маркера можно только выделив другой маркер, и только так,
убрать фокус с маркера нельзя, таким образом, если выбрать один из установленных маркеров,
то в последствии один из маркеров всегда будет выделенным и стрелки будут перемещать его.
при перетаскивании установленных маркеров разбивки на сегменты не отображается номер кадра над маркерами маркеров
 
 // Отладочный вывод (можно удалить после проверки)


            _messenger.Send(new Messages.PingMessage($"Fedor ExportSelected called with segment: {segment?.ToString() ?? "null"}"));
            _messenger.Send(new Messages.PingMessage($"Fedor ExportSelected called with segment: {_currentFilePath}"));
            _messenger.Send(new Messages.PingMessage($"RebuildSegments: Segments count = {Segments.Count}"));
            _messenger.Send(new Messages.PingMessage("Fedor ExportViewModel constructor called"));
            _messenger.Send(new Messages.PingMessage($"Fedor FileLoadedMessage received. Path={m.FilePath}")); // Отладочный вывод (можно удалить после проверки)
            
            _messenger.Send(new Messages.PingMessage("Fedor ExportSelected"));  // временный лог
            System.Diagnostics.Debug.WriteLine("=== ExportSelected command called ===");

            _messenger.Send(new Messages.PingMessage("Fedor MarkCUT")); // временный лог
            System.Diagnostics.Debug.WriteLine("=== System MarkCUT command called ===");

                        _messenger.Send(new Messages.PingMessage("Fedor ExportSelected"));
            System.Diagnostics.Debug.WriteLine("=== ExportSelected command called ===");

            System.Diagnostics.Debug.WriteLine("=== ExportAll command called ===");



[RAW KEY] Key=L Handled=True Modifiers=None
[RAW KEY] Key=J Handled=True Modifiers=None
[RAW KEY] Key=Delete Handled=True Modifiers=None
[RAW KEY] Key=Up Handled=False Modifiers=None
[RAW KEY] Key=Down Handled=False Modifiers=None